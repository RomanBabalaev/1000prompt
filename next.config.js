/** @type {import('next').NextConfig} */
const nextConfig = {
  // ... другие конфигурации ...
  // Например, если у вас есть специфические настройки, вы можете добавить их здесь
};

module.exports = nextConfig;